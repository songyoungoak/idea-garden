# CLAUDE.md

이 리포지토리에서 작업할 때의 규칙입니다. (사람·AI 공용)

## 이 리포지토리의 목적

아이디어를 **기록하고 목업(덱)까지만** 만드는 곳입니다.
- ✅ 하는 것: 아이디어 이슈 관리, 외부 공개용 프로젝트 개요 덱(HTML/PDF, KO/JA) 작성·업데이트
- ❌ 하지 않는 것: 실제 애플리케이션 구현. 개발은 아이디어가 성숙한 뒤 **별도 리포지토리**에서 진행합니다.

여기서 나온 덱은 그대로 포트폴리오로 쓰입니다. 톤은 "내부 메모"가 아니라 **외부에 설명할 수 있는 수준**으로.

---

## 아이디어 = 이슈

아이디어 하나당 GitHub 이슈 하나. 이슈가 아이디어의 단일 히스토리입니다.

**라벨 체계**
- `stage:*` — 성숙도 (필수, 1개만): `seed` → `sprout` → `grown` → `transplanted` / `archived`
- `idea:<slug>` — 아이디어 식별 태그. 덱 디렉터리명 `decks/<slug>/` 와 반드시 일치시킵니다.
- `domain:*` — 분야 (예: `domain:ai`, `domain:lang`, `domain:fintech`, `domain:local`)

새 아이디어를 이슈로 등록할 때는 `stage:seed` + `idea:<slug>` + `domain:*` 를 함께 붙이고,
README의 **아이디어 리스트 표에 한 줄을 추가**합니다. (이슈만 만들고 README를 놔두지 않습니다.)

**이슈 본문 템플릿**

```markdown
## 한 줄 정의
## 배경 / 문제
## 대상 사용자
## 무엇을 만드나 (구체적 목표)
## 기술적으로 고민되는 것
## 열린 질문
```

---

## 덱(Deck) 작성 규칙

**분량: 기본 3장, 최대 7장.** 7장을 넘기려 한다면 아이디어가 덜 좁혀진 것이니 먼저 좁힙니다.

**슬라이드 구성** (3장으로 줄일 땐 2·3·4를 한 장에 합칩니다)
1. Cover — 프로젝트명 / 한 줄 정의 / 현재 단계
2. 배경 · 문제
3. 개요 · 솔루션 (타깃 고객, 핵심 가치)
4. 기술적 고민
5. 아키텍처 선정 (선택지와 트레이드오프)
6. 현황 · 로드맵
7. 링크

**파일 구조**
```
decks/<slug>/
├── content.yaml        # KO/JA 텍스트를 모두 담는 단일 소스
├── index.ko.html
├── index.ja.html
└── dist/<slug>.{ko,ja}.pdf
```

**언어**
- 한국어판과 일본어판은 **슬라이드 수·구성이 동일**해야 합니다. 한쪽만 고치지 않습니다.
- 일본어판은 직역하지 말고 일본어 사용자에게 자연스러운 표현으로. 단, 기술 용어는 원어 유지.

**스타일**
- `templates/deck.html` 의 공통 템플릿/CSS를 씁니다. 덱마다 CSS를 새로 짜지 않습니다.
- 외부 CDN·폰트·이미지 호스트를 참조하지 않습니다. 전부 인라인 또는 로컬 파일. (오프라인/PDF 재현성)
- 슬라이드 1장 = 메시지 1개. 본문 텍스트는 슬라이드당 40단어 이내를 목표로.

**PDF 빌드**
슬라이드 크기는 CSS에서 고정합니다.
```css
@page { size: 1280px 720px; margin: 0; }
```
```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless --disable-gpu --no-pdf-header-footer \
  --print-to-pdf="decks/<slug>/dist/<slug>.ko.pdf" \
  "file:///Users/song/git-repo/idea-garden/decks/<slug>/index.ko.html"
```

---

## 리서치 문서 (research/)

덱을 만들기 전 단계의 조사 내용은 `decks/<slug>/research/` 에 HTML로 쌓습니다.
덱이 "외부 공개용 스냅샷"이라면, 리서치 문서는 **그 스냅샷을 만들기 위한 근거**입니다.

**파일 구조**
```
decks/<slug>/research/
├── index.html          # 문서 목록 + 확인된 핵심 + 미확인 항목
├── 00-<topic>.html     # 본문 문서
├── 01-<topic>.html
├── 01a-<subtopic>.html # ← 01 의 드릴다운 (아래 규칙)
└── 02-<topic>.html
```

**작성 원칙**
- **확인된 사실과 미확인을 반드시 구분합니다.** 모든 문서 끝에 `NEXT — 아직 확인하지 못한 것` 절을 둡니다.
  조사하다 만 것을 조사한 척 쓰지 않습니다.
- **출처를 전부 남깁니다.** 문서 하단에 번호를 매긴 출처 목록. 공식 자료(정부·재판소·국세청 등)에는 ★ 표시.
- **수치에는 조사 시점을 적습니다.** 금리·세제·시세는 분기 단위로 바뀝니다.
- 자료 간 수치가 어긋나면 **어느 한쪽을 고르지 말고 어긋난다는 사실 자체를 적고** 재확인 항목으로 넘깁니다.
- 참조한 자료의 기한이 이미 지났다면(예: "2026년 3월 31일까지"인데 오늘이 8월),
  그 사실을 경고 박스로 명시합니다. 자료가 갱신을 반영하지 않았을 수 있습니다.
- 법률·세무·금융 내용에는 **"자문이 아니다"** 는 취지를 문서 상단에 둡니다.

**드릴다운 문서**

본문에서 한 단계 더 깊이 들어가야 하는 주제는 **본문을 늘리지 말고 별도 HTML로 분리**합니다.
본문의 흐름(읽는 속도)을 지키면서 깊이를 확보하기 위해서입니다.

- 파일명 — 부모가 `01-auction.html` 이면 자식은 `01a-<subtopic>.html`, 다음은 `01b-...`
- 본문에서는 **해당 용어 자체에 링크**를 걸고 **새 창**으로 엽니다.
  ```html
  <a href="01a-arrears.html" target="_blank" rel="noopener">체납 관리비·수선적립금의 지불 의무</a>
  ```
  `rel="noopener"` 를 빠뜨리지 않습니다.
- 링크만 걸고 끝내지 말고, 그 문단 아래에 **무엇이 들어 있는지 한 줄 요약 + 별도 링크**를 함께 둡니다.
  용어 링크는 지나가다 눌리고, 요약 링크는 찾아서 눌립니다.
- 자식 문서는 **부모와 같은 템플릿·CSS**를 쓰고, 헤더 eyebrow에 `research NN-x · 드릴다운`,
  meta 줄에 **부모로 돌아가는 링크와 문서 목록 링크**를 둡니다.
- 자식 문서도 `index.html` 에 등재합니다. (등재하지 않으면 없는 문서가 됩니다)

**스타일**
- 리서치 문서끼리는 **동일한 CSS 토큰**을 씁니다. 새 문서를 만들 때 기존 문서에서 `<style>` 블록을 그대로 가져옵니다.
- 외부 CDN·폰트·이미지 호스트를 참조하지 않습니다. (덱과 동일)
- 라이트/다크 양쪽에서 읽히는지 확인합니다.
- 경고 수준을 색으로 구분합니다 — `b-crit`(반드시 알아야 할 함정) / `b-warn`(주의·검증 필요) / `b-info`(보조 설명).
  전부 빨강으로 칠하면 아무것도 강조되지 않습니다.

---

## 공개 / 비공개 경계 (중요)

덱과 이슈는 공개 리포지토리에 있습니다. 커밋 전 아래를 확인합니다.

**담아도 되는 것**
문제 정의 · 타깃 고객 · 가치 제안 · 기술 스택과 선택 이유 · 아키텍처 개요 · 마일스톤 · 공개 데모 링크

**담지 않는 것**
- API 키/토큰/엔드포인트, 인프라 식별자(계정 ID, 버킷명, 호스트명, 내부 IP)
- 수집 대상 사이트·계약 상세, 데이터 파트너·고객사 실명
- 투자 전략 파라미터, 알파 시그널, 백테스트 세부 수치
- 프롬프트 원문, 파인튜닝 데이터, 미공개 알고리즘의 핵심
- 개인정보, 미공개 매출/원가 수치

**애매하면 넣지 않습니다.** 덱은 주기적으로 업데이트되므로, 공개 가능해진 시점에 추가하면 됩니다.

---

## 작성 시 주의

- **추측해서 쓰지 않습니다.** 각 프로젝트의 내용을 모르면 리포 이름으로 유추해 채우지 말고,
  해당 리포지토리를 읽거나 사용자에게 물어봅니다. 확인 전에는 `_미작성_` 으로 둡니다.
- 진행 중 프로젝트의 덱을 업데이트할 때는 **해당 리포지토리의 현재 상태를 먼저 확인**하고 반영합니다.
- 숫자(사용자 수, 성능, 매출)는 근거가 있을 때만 씁니다. 없으면 쓰지 않습니다.

---

## 커밋

- `idea: <slug> 이슈 등록` / `deck: <slug> 초안 작성` / `deck: <slug> 현황 업데이트` / `docs: ...`
- 덱을 고치면 KO/JA HTML과 PDF를 함께 커밋합니다.
